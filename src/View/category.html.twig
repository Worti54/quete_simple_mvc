
{% extends "layout.html.twig" %}


{% block title %}MVC 5 categories{% endblock %}


{% block content %}
    <h1>Categories</h1>
    <ul>
        {% for category in categories %}
            <li>{{ category.name }}  <a href="categories/edit/{{ category.id }}" class="btn btn-info">Editer</a>  <a href="categories/delete/{{ category.id }}" class="btn btn-danger">Supprimer</a></li>

        {% endfor %}
    </ul>
    <a href="categories/add" class="btn btn-primary">ajouter une catégorie</a>
{% endblock %}